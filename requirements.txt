pymupdf==1.24.*
rank_bm25==0.2.*
scikit-learn>=1.3
numpy>=1.24
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pytesseract>=0.3.10
pdf2image>=1.17
python-docx>=1.1
anthropic>=0.34

# Optional -- real semantic embeddings and a cross-encoder reranker.
# Needs internet access to download the model on first use (see README
# "Environment notes"). Not required for the offline TF-IDF+SVD fallback.
sentence-transformers>=2.6
